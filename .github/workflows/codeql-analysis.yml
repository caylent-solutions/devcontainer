name: "CodeQL Analysis (Disabled)"

on:
  push:
    branches-ignore: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '41 22 * * 6'

jobs:
  analyze:
    name: CodeQL Skipped
    runs-on: ubuntu-latest
    if: ${{ false }}
    steps:
      - run: echo "CodeQL analysis is intentionally disabled in this workflow."
